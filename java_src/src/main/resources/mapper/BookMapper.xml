<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qfclass.novelReader.mapper.BookMapper">

    <select id="getHomeSelectionListByCid" resultType="com.qfclass.novelReader.domain.Book">
        select *
        from book a
        where a.cid = #{cid}
        and a.id in (select book_id from home_selection where state = 0)
        and a.state = 0;
    </select>
</mapper>
